const restar = (a,b) =>{
  return a - b
}

const sumar = (a,b) => {
  return a + b
};

//Si la funcion del objeto y la funcion de tu archivo se llaman igual, quitale el "= functionName".
export const aritmetica = {
  sumar,
  restar
}

